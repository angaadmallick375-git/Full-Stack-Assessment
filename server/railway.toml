[build]
builder = "nixpacks"
buildCommand = "npm install && cd ../client && npm install && VITE_API_URL=/api npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
